version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./backend/todo_app.db:/app/todo_app.db  # Persist database